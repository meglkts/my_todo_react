const { clearCompleted, markAllComplete } = require('../coordinators')
const { stateReducer } = require('../state_reducer')

describe('coordinators', () => {
  describe('coordinators.clearCompleted()', () => {
    it('should call stateReducer.deleteCompletedTasks', () => {

    })
  })
  describe('coordinators.markAllComplete()', () => {
    it('should call stateReducer.setAllTasksCompleted', () => {

    })
  })
})
