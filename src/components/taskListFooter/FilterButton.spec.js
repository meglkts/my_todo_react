const { createElement: r } = require('react')
const { deepEqual } = require('assert')

describe('FilterButton.js', () => {
  describe('FilterButton()', () => {
    it('should render a button with given onClick and filter text', () => {

    })
  })
})
