/*
toggle select all / deselect All
write task (on enter), {task}
select / deselect task (on click), {id}
edit task (double click) --> write task
delete task (on click), {id}
filter (on click), {filter}
clear completed (on click)
*/
